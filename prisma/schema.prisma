// INDAI Clean — Database Schema
// Models deferred until Lead System implementation (per MASTER_SPEC §12)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Deferred: Lead, Service, and other models will be added
// when the corresponding features are implemented per §12.
