/** @type {import('next').NextConfig} */
const nextConfig = {
  // Deferred: image domains, redirects, rewrites will be added per feature requirements
  reactStrictMode: true,
};

export default nextConfig;
